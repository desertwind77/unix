> Create a new repository
   git init
   git add -A
   git commit -m "first commit"
   git branch -M main
   git remote add origin https://github.com/desertwind77/config.git
   git push -u origin main

> Pushng an existing repository
   git remote add origin https://github.com/desertwind77/config.git
   git branch -M main
   git push -u origin main

> Patch the diff
   git diff
   git diff <filename>
   git diff <branch>
   git diff --staged [filename]
   git diff commit_id1 commit_id2
   git diff > my_diff.patch
   git apply my_diff.patch
